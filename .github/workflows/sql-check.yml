```yaml
name: SQL CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  sql-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check SQL scripts
        run: |
        test -f SQLQuery1.sql echo "SQL file found successfully" 
          
